[project]
name = "teoria-computacao-compiladores-2026-exercicios"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Marcelo Plinio", email = "mplmdswork@gmail.com" }
]
requires-python = ">=3.13,<3.14"
dependencies = [
    "automata-lib>=9.2.0",
    "graphviz>=0.21",
    "lark>=1.3.1",
    "ply>=3.11",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
